<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/LinearLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:layout_margin="5dp"
    android:orientation="vertical"
    tools:context=".Inicial" >

    <LinearLayout
        android:id="@+id/linearLayout1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="15dp"
        android:layout_margin="10dp" >

        <ImageButton
            android:id="@+id/fotografar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="abrefotografar"
            android:paddingBottom="15dp"            
            android:paddingTop="15dp"
            android:src="@drawable/maquina"
            android:text="@string/fotografar" />

        <ImageButton
            android:id="@+id/verlocal"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:onClick="abreVerlocal"
            android:src="@drawable/local"
            android:text="@string/verlocal"
            android:paddingBottom="15dp"            
            android:paddingTop="15dp" />

    </LinearLayout>

</LinearLayout>